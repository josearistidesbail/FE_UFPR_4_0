# KiCad local / generated state
*.kicad_prl
*.kicad_pro.lck
~*.lck
*-backups/
_autosave-*
*.bak
*-bak
fp-info-cache

# Fabrication / export outputs (regenerated in S12)
/gerbers/
/production/
/exports/
*.zip

# MCP snapshot checkpoints (duplicate git history)
/snapshots/
/logs/

# MCP server checkpoint repo — a stale checkpoint here overwrote the finished S2
# schematics once (see S3 session notes). Never track it; safest is to delete it.
/.history/
__pycache__/
*.pyc

# MCP server backups and renders
/.mcp-backups/
*_2d_view.png

# KiCad 10 Local History: its repo went corrupt (2026-08-25) and its restores rolled the project back twice; feature disabled 2026-09-23
/.history*/
_restore_backup*/
